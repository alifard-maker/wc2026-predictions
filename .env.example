# Local development
SECRET_KEY=change-me-to-a-long-random-string
PORT=5050

# Optional: override invite-link base (Railway sets this automatically via the request host)
# PUBLIC_URL=https://your-app.up.railway.app

# Railway production — mount a volume at /data and set:
# DATABASE_PATH=/data/predictions.db
